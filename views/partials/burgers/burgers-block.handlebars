{{!-- <li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if !devoured}}Eat this burger!{{else}}{{/if}}
	</button>
	{{!-- <button class="delete-cat" data-id="{{this.id}}">
		DELETE
	</button> --}}
</li> --}}